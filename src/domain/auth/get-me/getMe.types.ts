export type GetMeRequest = Record<string, never>;

export type GetMeResponse = {
  user: {
    id: string;
    email: string;
    name: string;
    image?: string | null;
    emailVerified: boolean;
    createdAt: Date;
    updatedAt: Date;
  };
};
